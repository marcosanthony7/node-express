{
  "name": "node-express-marcos",
  "version": "1.0.0",
  "description": "node -v         npm -v     ## Caso não encontre esse comando, instalar node         https://nodejs.org/     # Executar no terminal         npm init -y",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
